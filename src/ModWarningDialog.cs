// ModWarningDialog is now fully integrated into SaveSlotUIBehaviour (OnGUI).
// This file is intentionally left as a stub so the project compiles cleanly
// even if old references exist.
namespace SaveSlotsMod
{
    // intentionally empty — dialog logic lives in SaveSlotUIBehaviour.DrawWarning()
}
